# 日本 700 景點踩點圖鑑 — 北海道固定照片版

網站：https://aaaa0976355849-glitch.github.io/Train1/

## 本次完成：北海道 45 / 45

北海道 45 個景點均已配置一張固定實拍照片，45 個檔案皆不重複，總計 8,771,591 bytes。照片已下載並儲存在 `images/hokkaido/`，對照表為 `hokkaido-photos.json`。瀏覽北海道時只讀取本網站的對照表與照片檔，不呼叫 Wikipedia／Wikimedia 圖片搜尋 API，也不因找不到檔案而改成即時搜尋。

照片來源、作者與授權列在 `photo-credits.html`；每張卡片的署名直接連到對應段落。採用 CC BY、CC BY-SA、CC0 或公有領域照片，各自遵守原圖授權。縮圖與 4:3 裁切顯示不改變其原授權。照片拍攝時間不同，不代表今日景況；合併多處景點的清單項目以其中一處具代表性的景色配圖。

已逐張目視檢查本批照片。藻岩山與函館山選用夜景，TOMAMU 雲海平台使用從平台拍攝的雲海。其餘地區尚未完成固定配圖，暫時維持上一版的圖片查找方式；本次未宣稱全日本 700 張照片均已完成。

## 介面與紀錄保留

地區完成度在最上方、篩選列在下方。10 個地區可收合／展開，也支援全部展開／全部收合。卡片為圖片、名稱與勾選框；已勾選的卡片在各自地區自動排前面。

`app.js` 與 `data.1.txt`–`data.4.txt` 未修改：仍為原有 700 個景點、47 都道府縣。主紀錄沿用 `japan700-tracker-v1`；日期與備註雖不顯示，仍保留於原紀錄與備份。請使用原本同一裝置、同一瀏覽器，不要清除瀏覽資料來更新網站。紀錄是本機儲存，不是 GitHub 雲端同步。

## 驗證紀錄

2026-09-12，GitHub Actions 的 `Validate Hokkaido fixed photographs` 執行完成，100 / 100 項檢查通過。

測試使用 GitHub runner 的無頭 Chrome、真實本機 HTTP 圖片檔及瀏覽器 localStorage，分別採 1440×1000 桌面與 390×844 手機尺寸。包含全部 45 張圖片載入、無外部圖片搜尋請求、勾選與取消、排序、重新整理後紀錄保留、舊版紀錄相容、收合設定、篩選、搜尋與授權段落。這不是使用者手機 Safari 的實機測試；runner 截圖環境未安裝中文字型，截圖文字不能用來判定使用者裝置字型表現。

驗證流程：https://github.com/aaaa0976355849-glitch/Train1/actions/runs/34680938636

本次僅發布完成的 `japan-checklist/` 網站內容；原有 Pages 部署流程與根目錄 C# 專案保持不變。
