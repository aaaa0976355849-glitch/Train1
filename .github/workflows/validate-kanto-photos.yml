name: Validate Kanto fixed photographs
on:
  push:
    branches: [work/kanto-static-photos]
    paths:
      - 'tools/integrate_kanto.py'
      - 'tools/test_kanto.py'
      - '.github/workflows/validate-kanto-photos.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: kanto-photo-validation
  cancel-in-progress: false
jobs:
  validate:
    if: github.ref == 'refs/heads/work/kanto-static-photos'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Install browser test dependencies
        run: |
          python3 -m pip install playwright==1.57.0 Pillow==12.0.0
          sudo apt-get update -qq
          sudo apt-get install -y fonts-noto-cjk
          if ! command -v google-chrome && ! command -v chromium; then
            python3 -m playwright install chromium
          fi
      - name: Integrate fixed photos and attribution
        run: python3 tools/integrate_kanto.py
      - name: Verify real images and persistent browser records
        run: |
          node --check japan-checklist/photos.js
          node --check japan-checklist/app.js
          python3 tools/test_kanto.py
      - name: Commit verified website to review branch only
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add japan-checklist/photos.js japan-checklist/index.html japan-checklist/photo-credits-kanto.html
          if ! git diff --cached --quiet; then
            git commit -m 'Integrate and verify 116 fixed Kanto photographs and attribution'
            git push origin HEAD:work/kanto-static-photos
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: kanto-final-review
          path: |
            japan-checklist/
            kanto-tests/
          retention-days: 3
